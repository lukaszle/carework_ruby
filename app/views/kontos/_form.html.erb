<%= form_for(@konto) do |f| %>
  <% if @konto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@konto.errors.count, "error") %> prohibited this konto from being saved:</h2>

      <ul>
      <% @konto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br>
    <%= f.number_field :id %>
  </div>
  <div class="field">
    <%= f.label :konto %><br>
    <%= f.text_field :konto %>
  </div>
  <div class="field">
    <%= f.label :klient_id %><br>
    <%= f.text_field :klient_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
